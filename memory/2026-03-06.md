
## 15:51 AEST - Durable ops notes
- Investigated Frodex/Quandalf card mismatch: root cause was mixed run windows and dual emit paths causing apparent reversals/duplicates.
- Implemented cycle-pairing/card formatting improvements (single-line header with emojis, short display IDs), then reverted chain-control scripts to known-good baseline due hyper baton regressions.
- Restored chain control to baseline commit state for:
  - scripts/automation/run_autopilot_task.ps1
  - scripts/automation/check_quandalf_handoff.ps1
  - scripts/automation/set_hyper_mode.ps1
- Kept card/reporting upgrades in bundle-run-log and docs, plus gateway watchdog health/start logic updates.
- Added gateway watchdog health probe script behavior (start-only intent), but post-update discovered service control mismatch scenario.
- OpenClaw updated from 2026.2.19-2 to 2026.3.2 via npm global install.
- After update, gateway reported "device signature invalid" with service shown as missing; service install failed due non-admin permissions.
- Recovery action in progress: started gateway with `openclaw gateway run --force --bind loopback --port 18789` in background session `dawn-otter`.
- Need follow-up verification after compaction:
  1) confirm `openclaw gateway status` shows `RPC probe: ok`
  2) ensure Telegram ingress stable
  3) revisit watchdog to handle non-service mode safely under non-admin environments.
