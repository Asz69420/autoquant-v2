## 2026-03-10 08:20 AEST — Research/refinement pipeline repair
- Research pipeline root cause was a nested self-call to Oragorn inside `pipelines/research-cycle.lobster` (`oragorn-review` step calling `openclaw agent --agent oragorn ...`). This wedged research before fresh `cycle_orders.json` and `current_cycle_specs.json` could update.
- Replaced that self-call with deterministic `scripts/build_cycle_orders.py` so each research cycle writes fresh cycle orders directly from `briefing_packet.json` + supported candle universe.
- Added `scripts/fallback_cook_specs.py` so cooking always emits a valid batch if the interactive Quandalf design step produces zero specs. Fallback writes 3 baseline specs for the active cycle and now uses cycle-specific ids (`QD-YYYYMMDD-C<cycle>-...`) so `capture_cycle_specs.py` sees fresh files each run.
- Verified repaired cooking path end-to-end on cycle 222: fresh `cycle_orders.json`, fresh manifest with 3 specs, `parallel_runner.py` seeded and completed 3 screen jobs, `build-reflection-packet.py` and `cycle-postprocess.py` ran, and the cooking/log card was sent.
- Research cron timeout increased to 1200s during debugging, but real functional fix was removing nested self-call and restoring deterministic cooking. Refinement remains healthy and now depends on cooked results existing first.
- Refinement banner image updated to `assets/banners/refinement.jpg`, and `scripts/refinement_cycle.py` now attaches that banner automatically when sending refinement log cards.
- YouTube digest work also completed today: split feed into `agents/quandalf/memory/research-digest-trading.md` and `docs/shared/ORAGORN_DIGEST.md`, re-enabled trading channels by fixing URL/channel-id config, added feed targets (`quandalf` vs `oragorn`), added RileyBrown as Oragorn feed, and changed watcher auto-disable logic to require 3 failed fetches within a rolling 24h window.
