
## Oragorn durable memory append — 2026-03-10 evening

- Pipeline hardening completed today:
  - blocked zero-trade walk-forward results from being written to DB
  - integrity skips now surface as worker failures instead of fake successful results
  - DB cleanup removed 516 poisoned rows (506 zero-trade, 10 impossible PF>1 with WR=0); backup stored in artifacts/db_backups/
  - watcher routing now: routine health cards stay in log channel, failures -> Hades (-5133891354) + Oragorn DM, repeated critical -> Hades + Oragorn + Asz
  - cycle-postprocess now raises an integrity alert if a cycle has results but none are healthy enough for reflection

- Queue/funnel hardening completed today:
  - stale running queue jobs auto-reset
  - orphan legacy queued work cleanup helper added
  - fallback cooker now treats under-minimum spec output as failure, and emits 3 rescue specs instead of 1
  - refinement cycle now skips abandoned families and repeated weak refinements more aggressively

- Current live blocker is NOT idea freshness; it is execution throughput / conversion.
  - Recent cycles are writing 3 fresh explore specs, but many cycles still show 0 completed backtests for their own cycle window.
  - Example at flush time: cycle 304 wrote 3 ETH/4h specs, but cycle-postprocess reported no new backtest results in the last 30 minutes.
  - Gateway errors remain elevated and continue to be flagged by Logron.

- Council debate completed and written to council_debate.md.
  Consensus across Quandalf, Frodex, and Logron:
  1. State integrity + canonical cycle/lineage truth first
  2. Hard refinement gates + loser killing second
  3. Broader guided exploration (family/mechanism + trade-management diversity) third
  Core principle: make the loop truthful first, then fast.

- Important unresolved work for next session:
  1. Audit/fix refinement truth layer because refinement log cards appear to show zeros and may be lying OR refinement truly has no meaningful eligible jobs.
  2. Build canonical cycle/run record tying orders -> specs -> queue jobs -> backtests -> reflections -> lessons.
  3. Add stage KPI visibility: candidates -> valid specs -> queued -> completed -> pass -> promote.
  4. Investigate why research cycles keep producing specs without their own backtests completing inside expected window.

- Operational note:
  - Asz manually reset the gateway during troubleshooting.
  - Codex CLI is now installed and available (`codex exec` works; do not combine `--full-auto` with `--dangerously-bypass-approvals-and-sandbox`).

## Oragorn durable memory append — 2026-03-10 late evening

- I fixed the specific queue bug where `parallel_runner.py` returned `no_work` if a rerun seeded no new specs, even when that cycle already had queued jobs. Reruns now process existing queued work for the target cycle.

- I completed the next hardening pass on truthful execution accounting:
  - `walk_forward_engine.py` now exposes `integrity_issue.reason` in stdout JSON so upstream queue logic can classify failures correctly.
  - `parallel_runner.py` now treats zero-trade integrity skips (`zero_trades_both_samples`, etc.) as terminal queue failures, marks those jobs `done` instead of re-queuing them, and logs terminal-vs-retryable failure metadata.
  - `cycle-postprocess.py` now reads real queue state for the active cycle and writes canonical stage KPIs into `current_cycle_metrics.json` / batch summary: `queue_total`, `queue_pending`, `queue_running`, `queue_done`, `queue_done_ok`, `queue_terminal_failures`, `queue_retryable_failures`, plus `stage_kpis`.
  - `refinement_cycle.py` cards now report `Requested / Ran / OK / Fail` instead of ambiguous backtest counts.

- I reran stuck rescue queues for cycles 308, 309, 310, and 311 manually.
  - All previously stuck fallback rescue jobs now resolve honestly as terminal zero-trade failures instead of sitting in queued/retry limbo.
  - Verified examples: BTC/4h cycle 311 rescue families now end as `done` with `integrity_skip:zero_trades_both_samples` and no further requeue.

- The current truth is clearer now:
  - the queue/execution hole was real and is fixed,
  - but the fallback rescue families themselves are weak and produce zero trades,
  - so the next frontier is improving spec generation quality rather than more queue debugging.

- Fallback generator was upgraded in code to emit 3 denser rescue families with diversified management styles (`one_shot`, `partial_runner`, `trail_and_timer`) instead of reusing a thin one-shot pattern.
  - However, live cycle state still showed old fallback copy in `current_cycle_status.json` / metrics because the active cycle had already been generated before the code change; expect the new wording/behavior on subsequent cycles, not retroactively.

- Commit landed for this hardening pass:
  - `93e57e3` — `Fix cycle truth and stop retrying zero-trade queue failures`

## Oragorn durable memory append — 2026-03-10 memory-governance v2 build

- Asz approved building the upgraded memory system in-place, native-first, with no Obsidian/human-facing layer required. Priority is AI efficiency, automation, low drift, and long-term reliability.

- Memory Governance V2 backbone is now live:
  - added `config/memory-governance.json`
  - added schemas: `schemas/task-board.schema.json`, `schemas/completion-record.schema.json`
  - added strategic docs: `ROADMAP.md`, `docs/MEMORY_GOVERNANCE_V2.md`
  - added automation scripts: `scripts/task_governor.py`, `scripts/memory_governor.py`
  - bootstrapped shared memory at `memory/shared/` with buckets for: lessons, decisions, known_fixes, regimes, strategy_families, postmortems, handoffs
  - bootstrapped shared index at `memory/shared/INDEX.json`
  - bootstrapped deterministic task state at `data/state/task_board.json`
  - bootstrapped proof trail at `artifacts/completions/`

- Architectural direction locked in:
  - keep SQLite as the hard factual layer
  - keep canonical rules in top-level/config files
  - use shared intelligence memory for promoted lessons/decisions/fixes
  - keep agent-private memory bounded and role-specific
  - use task/completion governance to keep roadmap progress self-updating
  - use few durable agents + many reusable skills/tools + temporary subagents only for bounded heavy work

- Initial task spine seeded and live:
  - TASK-0001 `Bootstrap memory governance spine` — done
  - TASK-0002 `Bootstrap shared intelligence memory buckets` — done
  - TASK-0003 `Backfill Quandalf lessons into shared intelligence` — active
  - TASK-0004 `Integrate completion records into recurring pipelines` — queued
  - TASK-0005 `Design external market intelligence ingestion for Quandalf` — queued

- First promoted shared-memory decision:
  - `Memory Governance V2 Architecture Adopted`
  - stored under `memory/shared/decisions/`

- First promoted shared-memory lesson from Quandalf:
  - `Intraday Strategy Grammar Must Be Broader and Less Event-Driven`
  - derived from latest journal Entry 092
  - core lesson: intraday strategy language is still too sparse/event-driven; future intraday design should prefer broader continuous state expressions with less ceremonial confirmation stacking

- User-facing preference clarified:
  - Asz does want a persistent self-managed roadmap/task system that agents can keep updated automatically after significant work
  - roadmap should be canonical and not freely rewritten by every agent
  - agents may propose/update task state; governance layer/approved pipeline should maintain canonical roadmap truth

- Model/tooling note:
  - MiniMax M2.5 monthly plan was discussed as an optional cheap secondary coding model. Conclusion: potentially useful extra horsepower, but not required for the architecture and should not become a core dependency unless later proven valuable.
