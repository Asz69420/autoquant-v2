# 2026-03-09 Durable Memory

## Gateway / Safety
- Added hard gateway lifecycle rule to `SOUL.md` and `AGENTS.md`: never run `openclaw gateway stop/start/restart/install`; gateway is externally watchdog-managed; if restart truly needed, escalate to Asz for manual restart.
- Implemented local watchdog scripts in `C:\Users\Clamps\.openclaw\scripts\`:
  - `gateway-launcher.vbs` (hidden launcher)
  - `gateway-watchdog.ps1` (infinite loop, backoff ladder 10/30/60/120/300s, healthy-run reset after 5m, 30m heartbeat logs)
- Scheduled-task creation failed with access denied in this runtime; added HKCU startup fallback:
  - `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\OpenClawGatewayWatchdog = wscript.exe "C:\Users\Clamps\.openclaw\scripts\gateway-launcher.vbs"`

## Messaging / Channels
- `scripts/tg_notify.py` now supports 3 channels: `dm`, `log`, `hades`.
- Routing defaults:
  - `log` -> `LOG_CHANNEL_ID` default `-5038734156`
  - `hades` -> `HADES_CHANNEL_ID` default `-5133891354`
  - `dm` -> `ASZ_CHAT_ID` default `1801759510`
- `.env` now includes:
  - `LOG_CHANNEL_ID=-5038734156`
  - `HADES_CHANNEL_ID=-5133891354`
- Cron announce delivery was globally redirected to Hades in cron jobs config (`to: -5133891354`).

## Research Pipeline / Automation
- `research-cycle.lobster` updated to include commander review step (`oragorn-review`) and explicit non-generic orders for Quandalf.
- Added `logron-health` step after `refine-postprocess` and before `xqs-check`.
- Removed `chain-next` self-trigger step after stability issues; pipeline now relies on cron queueing.
- Research cron (`b87f5776-64fa-4f4b-823d-e24cc8110925`) ultimately set to `*/1 * * * *` in `cron/jobs.json` for continuous non-overlapping cadence via queue.
- `cron.maxConcurrentRuns` set to `1` in `openclaw.json`.

## Quandalf / Dual-Brain
- Quandalf agent bound to Telegram account `quandalf` with allowlist for Asz.
- Quandalf model set to `openai/gpt-5.3-codex` in OpenClaw config; deep thinking delegated to Claude Code.
- Claude Code default model persisted in `C:\Users\Clamps\.claude\settings.json` as `claude-opus-4-6`.
- `tools.exec.host` set to `gateway`.
- Updated Quandalf prompts in pipeline to require Claude Code usage for design/reflect reasoning.
- Added "Deep Reasoning (Claude Code)" and Oragorn reporting guidance into `agents/quandalf/SOUL.md`.

## Logron Agent Wiring
- Created `C:\Users\Clamps\.openclaw\workspace-logron\` with:
  - `SOUL.md`
  - `AGENTS.md`
  - `HEARTBEAT.md`
- Patched `openclaw.json` to add Logron as full agent, Telegram account binding, and heartbeat (`every: 30m`, `target: none`).
- Expanded `tools.agentToAgent.allow` target set to include `oragorn`, `logron`, `quandalf`, `frodex`.

## Known Runtime Constraint
- In multiple later turns, direct `exec` calls from this session intermittently failed with `exec denied: allowlist miss`; when blocked, file-level verification via `read/edit` was still possible.

## 2026-03-09 Automation Recovery Notes
- OpenClaw was upgraded from `2026.3.2` to `2026.3.7` via global npm update after stopping locking Node/OpenClaw processes. Doctor now reports no Telegram channel security warnings.
- Telegram allowlist delivery bug was repaired in `C:\Users\Clamps\.openclaw\openclaw.json` by adding Asz sender IDs to both top-level and `channels.telegram.accounts.default` allowlist paths (`groupAllowFrom` and `allowFrom`). Cron delivery now works again; later `cron runs` showed `delivered: true` for active research job notifications.
- Default/runtime model was switched to `openai-codex/gpt-5.4` in `openclaw.json` for Oragorn/default model selection.
- Root-cause audit found the major automation fault: scheduled research execution had drifted away from deterministic canonical pipeline execution, and canonical backtest handoff was broken because `research-cycle.lobster` called `parallel_runner.py --spec LATEST_SPEC` without a real resolver.
- Canonical pipeline repair was applied:
  - `pipelines/research-cycle.lobster` now includes `cycle-start`, `capture-cycle-specs`, and manifest-based backtest step.
  - Backtest step now calls `parallel_runner.py --manifest data/state/current_cycle_specs.json --variant all` instead of `LATEST_SPEC`.
  - Research cron job in `C:\Users\Clamps\.openclaw\cron\jobs.json` now points back to canonical `research-cycle.lobster` with `lobster run --file ...` and timeout instead of the earlier vague drifting agent instruction.
- New helper script added: `scripts/cycle-start.py` writes `data/state/research_cycle_started_at.json` so cooking/runtime cards can measure full cycle elapsed time from true start.
- `scripts/parallel_runner.py` was updated to support deterministic manifest/latest-spec resolution using `data/state/current_cycle_specs.json`; `LATEST_SPEC` now resolves via manifest instead of falling through to placeholder behavior.
- `scripts/cycle-postprocess.py` now reads the cycle start marker for elapsed time; cooking log card remains on `log` channel. Cycle results + noisy per-cycle chatter were redirected to `hades` rather than `log`/DM. Proper daily DM paths remain: Oragorn daily intel -> DM, Quandalf daily journal cron -> DM.
- During recovery, a non-canonical `pipelines/research-cycle-cron-safe.lobster` path was created and used temporarily; later repair explicitly restored `research-cycle.lobster` as canonical scheduled path. If further cleanup happens, this safe file should be treated as non-canonical and not used for research cron.
- YouTube watcher was extended for Oragorn-specific architecture learning:
  - added channels `OpenClawNewsFactor`, `IBMTechnology`, and `Openclawlabs` to `data/state/youtube_watchlist.json`
  - created `data/state/youtube_channel_categories.json` with separate architecture/system categories (`OPENCLAW_ARCHITECTURE`, `AI_SYSTEMS` via review mapping)
  - patched `skills/autoquant-youtube-watcher/watcher.py` with `bootstrap_count` support so a newly added channel can intentionally ingest its first N videos
  - `Openclawlabs` was configured with `bootstrap_count: 3` to queue its first 3 videos into Oragorn’s architecture knowledge stream.
- Agent feedback architecture status at this point: `quandalf` and `logron` definitely write into `data/state/agent_messages.json`, and daily intel reads that board; full all-agent automated self-healing loop was still not proven healthy end-to-end during this session.
- Final verified state before compaction: delivery/routing/model upgrade improved, but full automation stack was still under repair/verification and not yet honestly all-green. Remaining high-risk areas were full cooking/backtest reliability, canonical run proof, scheduler hygiene, and complete self-healing verification.

## 2026-03-09 Late Session Findings
- Lobster on Windows is still not production-ready on this box. Verified facts:
  - `lobster --version` returned `2026.1.24`.
  - Global install path is `C:\Users\Clamps\AppData\Roaming\npm\node_modules\@clawdbot\lobster`.
  - Windows shell patch was applied in Lobster runtime files (`dist/src/commands/stdlib/exec.js`, `dist/src/sdk/primitives/exec.js`) to replace remaining `/bin/sh` usage with PowerShell invocation.
  - Even after the shell patch, `lobster run --file ...research-cycle.lobster --dry-run` still failed/hung, so `/bin/sh` was not the root blocker.
- `pipelines/research-cycle-cron-safe.lobster` is only a reduced legacy flow and is NOT a full replacement for canonical `research-cycle.lobster`; it omits core design/backtest/reflection chain steps.
- Research cron job ID changed. Current live research cron ID is `a6d9c3e4-2dc2-4a92-8b42-2c5bd7e62111` (`research-pipeline`). Old ID `b87f5776-64fa-4f4b-823d-e24cc8110925` is obsolete.
- Missing Quandalf files were identified and created as part of recovery expectations:
  - `agents/quandalf/MEMORY.md`
  - `agents/quandalf/memory/2026-03-09.md`
  - `agents/quandalf/memory/2026-03-08.md`
- Critical auth/model mismatch discovered in `C:\Users\Clamps\.openclaw\openclaw.json`:
  - `openai/gpt-5.3-codex` fails on this machine because runtime auth is OpenAI Codex OAuth, not OpenAI API key auth.
  - Correct OAuth-capable model string is `openai-codex/gpt-5.4`.
  - OpenClaw config was switched back so `primary`, `main`, `oragorn`, `frodex`, `quandalf`, and `logron` all use `openai-codex/gpt-5.4`.
- Claude Code CLI is separate from OpenClaw model config. Its local config remains `C:\Users\Clamps\.claude\settings.json` -> `claude-opus-4-6`; it cannot be set to GPT-5.4 via OpenClaw config alone.
- Practical conclusion from live debugging: do NOT depend on Lobster as the production orchestrator yet. Recommended short-term production path is ordinary cron + direct script execution for research/spec-capture/backtest flow, while Lobster remains a test lane until its Windows/runtime compatibility is proven.
- By session end, backlog symptom remained severe: 53 unbacktested strategy specs, no reliable new backtest flow from canonical research cron, and Quandalf’s standing warning remained active (30-day hit rate 0%, PF-mirage contamination still distorting evaluation).

## 2026-03-09 Current Recovery State Addendum
- Backtest integration with the new walk-forward engine is now fixed and proven live. Root causes repaired:
  - orchestration scripts still referenced the retired old backtester path,
  - `parallel_runner.py` still expected old artifact-file output contract,
  - `walk_forward_engine.py` DB inserts did not match live `backtest_results` schema.
- Files repaired for walk-forward integration:
  - `scripts/parallel_runner.py`
  - `scripts/cycle-postprocess.py`
  - `scripts/grind.py`
  - `scripts/xqs_runner.py`
  - `scripts/walk_forward_engine.py`
- Proof of real orchestrated backtest after repair:
  - manifest-driven runner succeeded on `QD-20260309-VVV-FUNDING-SNAPBACK-v12`
  - result id `wf_276395548317`
  - DB write succeeded with `db_saved=true`
  - local commit: `9a77b60` (`fix: align backtest runners with walk-forward engine`)
- Legacy spec compatibility with walk-forward was also fixed inside `scripts/walk_forward_engine.py`:
  - normalized legacy parameter arrays -> dicts
  - long/short directional rule objects
  - legacy `risk_policy` / position sizing / ATR exits
  - added missing condition support and aliases (`crosses_below`, Vortex aliases, dynamic EMA, Donchian, CHOP)
  - local commit: `0b2c4d8` (`fix: normalize legacy walk-forward strategy specs`)
- Remaining invalid legacy cases are now mostly real data/model constraints, not parser bugs:
  - funding-rate families need FundingRate input wiring
  - some families still fail because they lack enough candle history for WFA minimums
- Lobster chaining for the research/backtest path was re-established at the orchestration layer:
  - `pipelines/research-cycle.lobster` backtest step now calls `parallel-backtest.lobster`
  - `pipelines/parallel-backtest.lobster` now uses manifest-based runner (`current_cycle_specs.json`)
  - production research cron already points at `research-cycle.lobster`
  - commit: `2690164` (`route research backtests through lobster subpipeline`)
- Cooking/log card reporting fixes applied in `scripts/cycle-postprocess.py`:
  - card backtest count now derives from actual cycle-window result rows instead of only optional backfill path
  - duplicate same-cycle/same-content cards are suppressed with hash-based idempotency
  - commit: `c7d1122` (`fix: make cooking card backtest count cycle-aware`)
- Hades forwarding fix applied:
  - Quandalf journal delivery failed because `latest_journal.md` had non-UTF8 bytes and strict UTF-8 decode was silently swallowed
  - `cycle-postprocess.py` now does best-effort decode, HTML escaping, and truthfully reports send success
  - commit: `edfb794` (`fix: make Quandalf journal delivery decode-safe`)
- Quandalf anti-fixation doctrine was added:
  - inserted `Research Philosophy: Explorer, Not Optimizer` before Strategy Evaluation in `agents/quandalf/SOUL.md`
  - Oragorn order-writing prompts updated to enforce soft 70/30 explore/exploit balance and concept rotation via journal review
  - commit/push: `9c6ed13` (`steer quandalf toward exploration and rotation`)
- Cycle card metrics / throughput wiring were fixed:
  - `cycle-postprocess.py` no longer depends on stale `agents/quandalf/memory/strategy_status.json`
  - current-cycle metrics now come from manifest + cycle-aware status files; stale `abandoned=8` should no longer bleed through
  - exploration cycles are now explicitly prompted to produce 2-4 materially distinct specs; exploitation cycles 1-3 focused variants
- Cycle timing/reporting accounting was fixed:
  - `scripts/cycle-start.py` now allocates cycle ID and writes full run-state record
  - `scripts/research-cycle.py` reuses that cycle ID instead of incrementing separately
  - `scripts/cycle-postprocess.py` freezes true run duration once and separates later report delay
  - commit: `67563a2` (`fix cycle timing and reporting accounting`)
- Gateway/noise cleanup partially completed:
  - bogus `workspace-logron` and `workspace-main` ENOENT heartbeat noise was fixed outside repo by rewriting `workspace-logron/HEARTBEAT.md` to use shared health script and creating missing `workspace-main/memory/2026-03-09.md`
  - direct Telegram delivery path itself was proven working; earlier missing cooking/Hades visibility was mostly reporting-path bugs, now fixed
- Current high-priority live blockers discovered at end of session:
  1. `C:\Users\Clamps\.openclaw\cron\jobs.json` became malformed and cron began logging parse errors (`cron: timer tick failed`) around 12:29-12:30.
  2. Lobster runtime/tool is failing to spawn Node with `spawn C:\Program Files\nodejs\node.exe ENOENT`, which blocks research pipeline launches.
  3. Gateway still shows persistent warning/error noise from:
     - `tools.profile (coding) allowlist contains unknown entries (apply_patch, cron)`
     - `agents.oragorn.tools.allow` unknown entries `(glob, grep, ls)`
     - `exec` safeBins / hardened profile warnings for `python`
     - Telegram polling stall / lane wait exceeded / gateway congestion warnings
  4. Additional ENOENT noise remains from missing `C:\Users\Clamps\.openclaw\agents\main\agent\SYSTEM.md` and `DEVELOPER.md`.
- Best end-of-session truth: backtesting and walk-forward integration are repaired, but automation still is not fully well-oiled because cron parsing and Lobster Node spawn are now hard blockers, and gateway congestion/warning noise remains materially unresolved.

## 2026-03-09 Late-Night Concurrency / Delegation / Verification Addendum
- Anti-compaction protection was expanded across the repo:
  - added `SCRATCH.md` files with `No active task.` at workspace root and agent workspaces in-repo
  - added explicit `## Anti-Compaction: SCRATCH.md` guidance to all in-repo agent `SOUL.md` files
  - Oragorn `SOUL.md` now also includes explicit guidance to delegate one-off build tasks to isolated sub-agents/background coding agents rather than letting main-session work get stomped by cron/compaction.
- Host OpenClaw delegation config was partially repaired in `C:\Users\Clamps\.openclaw\openclaw.json`:
  - `tools.agentToAgent.allow` already included `oragorn`, `quandalf`, `frodex`, `logron`
  - Oragorn `subagents.allowAgents` was expanded to include `frodex`, `quandalf`, and `logron`
  - agent defaults now include `subagents.runTimeoutSeconds = 900`
  - `agents.defaults.maxConcurrent` was raised from `1` to `3`
  - `cron.maxConcurrentRuns` was raised from `1` to `2`
  - `agents.defaults.thinkingDefault` verified as `low`; no agent had explicit higher-than-low thinking overrides.
- Cron/session isolation findings:
  - all recurring jobs in `C:\Users\Clamps\.openclaw\cron\jobs.json` were already `sessionTarget: "isolated"`
  - research and refinement cron jobs were already time-staggered, but by the end of session both `research-pipeline` and `refinement-pipeline` were disabled in cron config.
- Backtest lane concurrency fix was committed/pushed in repo:
  - `scripts/parallel_runner.py` now caps stage worker fan-out to screen=3, full=2, validation=2
  - commit: `751b2e8` (`cap parallel backtest worker concurrency`)
  - practical goal: isolated cron backtests no longer monopolize gateway capacity or spawn too many local Python workers.
- Verification sweep outcomes that are worth preserving:
  - DB schema check passed: `backtest_results` contains the required family/regime/refinement columns; `research_funnel_queue` and `mechanism_priors` tables exist.
  - Walk-forward dry run works.
  - Cheap screen path works and writes screen-stage rows to DB in sub-second time (~0.27s on tested ETH/4h run).
  - Regime tagger works; `data/regime_cache/eth_4h.json` was generated with regimes represented: `CHOP`, `TRANSITION`, `TREND_DOWN`, `TREND_UP`.
  - Agent ping test status from this session: `oragorn`, `frodex`, and `logron` responded cleanly to a simple OK probe; `quandalf` was reachable but did not cleanly obey the minimal probe prompt.
- Real root-cause pipeline break found and fixed in repo:
  - `scripts/cycle-postprocess.py` was failing on `sqlite3.OperationalError: database is locked` while updating `mechanism_priors`
  - repaired by using explicit SQLite timeouts / busy timeout and clean commit-close sequencing for portability/priors updates
  - commit/push: `af3daa6` (`harden cycle postprocess sqlite writes`)
  - after fix, `cycle-postprocess.py --since-minutes 180` completed successfully, updated priors, sent log card, and extracted lessons.
- Current operational truth at compaction boundary:
  - gateway error load remained the main active issue flagged by Logron; latest observed warning was `High gateway errors (2166)`
  - cycle bookkeeping is still messy/inconsistent across state files (`manifest_cycle_id`, `status_cycle_id`, and current card cycle IDs drifted)
  - autonomous research/refinement cron is not truly live because both main pipeline cron jobs were disabled by end of session
  - YouTube digest split requested by Asz was NOT truthfully completed/proven; current research digest path is still mixed, several YouTube channels remain auto-disabled after fetch failures, and separate Oragorn-vs-Quandalf digest routing still needs a clean end-to-end implementation/proof.
