- Fixed multiple automation-chain bugs in cycle orchestration: corrected cycle-postprocess stage accounting (train/test counts from actual stages, ignore orphan-discard rows, strategy-level pass/iterate/abort counts), patched parallel_runner to loop screen/full/validation selection until no progress instead of a single screen pass, hardened capture_cycle_specs to fail on cycle-id drift and to accept both `-C67-` and zero-padded `-C067-` spec IDs.
- Hardened handoff coherence: build_cycle_orders now resolves cycle_id from active run state instead of stale briefing_packet; capture_cycle_specs now waits up to 150s for spec files after design if status announces specs, and triggers a reflection refresh when the reflection packet cycle lags.
- Patched cycle-postprocess decision self-heal: if reflection_packet cycle mismatches current cycle, it now runs build-reflection-packet before trying decision closure. Verified cycles 66 and 67 can complete design -> capture -> backtest -> reflection -> decisions -> postprocess with run_state marked completed.
- Important remaining root cause: there is still no explicit reflection/postprocess -> next research cycle trigger. The system can complete a single cycle, but it does not self-propel into the next research cycle after reflection; this is the main unresolved automation gap Asz cares about.
- Log-card routing evidence: tg_notify direct send to log channel `-5038734156` with bot `logron` succeeded live (Telegram accepted message_id 719). cycle_postprocess_card_state records reflection cards for cycles 64, 66, and 67, but Asz reports not seeing a clean real-world chain; likely issue is not basic Telegram delivery but end-to-end loop behavior and/or destination expectations.
- Current cycle facts: cycle 67 was a real completed run (not stale fake state). It produced 3 specs (DOGE/ETH/TAO 4h), all 9 screen rows integrity-skipped with zero trades, 0 healthy DB backtests saved, reflection/decision closure completed, run_state closed as completed. Current current_cycle_status.json still shows pending next_cycle_focus/rationale in some states, while reflection_packet and refinement_decisions for cycle 67 are present.
- Strategic quality signal remains poor: latest Quandalf journal/reporting shows repeated sparse ideas and aborts; cycle 66 had 3 aborts (2 too sparse, 1 bad idea), cycle 67 had 3 aborts from zero-trade sparse setups. Asz is frustrated specifically because he has not observed a single clean reflection -> next research automated chain.
