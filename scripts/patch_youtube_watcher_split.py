from pathlib import Path

p = Path(r"C:\Users\Clamps\.openclaw\skills\autoquant-youtube-watcher\watcher.py")
s = p.read_text(encoding='utf-8')

s = s.replace(
"CATEGORY_CONFIG_PATH = ROOT / 'data' / 'state' / 'youtube_channel_categories.json'\nCONCEPT_CATEGORIES = {'TRADING_CONCEPT', 'NUANCED_CONCEPT', 'MARKET_STRUCTURE', 'RISK_EXECUTION', 'MACRO_CONTEXT'}\nAUTO_CLASSIFY_CATEGORIES = ['INDICATOR_CONCEPT', 'TRADING_CONCEPT', 'NUANCED_CONCEPT', 'MARKET_STRUCTURE', 'RISK_EXECUTION', 'MACRO_CONTEXT']\n",
"CATEGORY_CONFIG_PATH = ROOT / 'data' / 'state' / 'youtube_channel_categories.json'\nQUANDALF_DIGEST_PATH = ROOT / 'agents' / 'quandalf' / 'memory' / 'research-digest-trading.md'\nORAGORN_DIGEST_PATH = ROOT / 'docs' / 'shared' / 'ORAGORN_DIGEST.md'\nDIGEST_ENTRY_CAP = 25\nFETCH_FAIL_WINDOW_SECONDS = 24 * 60 * 60\nCONCEPT_CATEGORIES = {'TRADING_CONCEPT', 'NUANCED_CONCEPT', 'MARKET_STRUCTURE', 'RISK_EXECUTION', 'MACRO_CONTEXT'}\nAUTO_CLASSIFY_CATEGORIES = ['INDICATOR_CONCEPT', 'TRADING_CONCEPT', 'NUANCED_CONCEPT', 'MARKET_STRUCTURE', 'RISK_EXECUTION', 'MACRO_CONTEXT']\n")

anchor = "def _ensure_state_dir():\n    (ROOT / 'data' / 'state').mkdir(parents=True, exist_ok=True)\n\n\n"
insert = '''def _ensure_state_dir():\n    (ROOT / 'data' / 'state').mkdir(parents=True, exist_ok=True)\n\n\ndef _read_digest_entries(path: Path) -> list[dict]:\n    if not path.exists():\n        return []\n    text = path.read_text(encoding='utf-8')\n    parts = text.split('---ENTRY---\\n')\n    out = []\n    for chunk in parts[1:]:\n        chunk = chunk.strip()\n        if not chunk:\n            continue\n        lines = chunk.splitlines()\n        meta = {}\n        body = []\n        in_body = False\n        for line in lines:\n            if not in_body and line.startswith('meta: '):\n                try:\n                    meta = json.loads(line[6:])\n                except Exception:\n                    meta = {}\n                continue\n            if not in_body and line == '':\n                in_body = True\n                continue\n            body.append(line)\n        if meta:\n            meta['body'] = '\\n'.join(body).strip()\n            out.append(meta)\n    return out\n\n\ndef _write_digest_entries(path: Path, title: str, subtitle: str, entries: list[dict]):\n    path.parent.mkdir(parents=True, exist_ok=True)\n    lines = [title, subtitle, '']\n    for e in entries[:DIGEST_ENTRY_CAP]:\n        body = str(e.get('body') or '').strip()\n        meta = {k: v for k, v in e.items() if k != 'body'}\n        lines.append('---ENTRY---')\n        lines.append('meta: ' + json.dumps(meta, ensure_ascii=False))\n        lines.append('')\n        if body:\n            lines.append(body)\n        lines.append('')\n    path.write_text('\\n'.join(lines).rstrip() + '\\n', encoding='utf-8')\n\n\ndef _digest_target_for_channel(ch: dict) -> str:\n    target = str(ch.get('feed_target') or '').strip().lower()\n    if target in {'quandalf', 'oragorn'}:\n        return target\n    mode = str(ch.get('mode') or '').strip().upper()\n    if mode in {'OPENCLAW_ARCHITECTURE', 'AI_SYSTEMS', 'AI_TOOLING', 'SECURITY'}:\n        return 'oragorn'\n    return 'quandalf'\n\n\ndef _build_digest_entry(ch: dict, item: dict, rc_path: str, content_category: str) -> dict:\n    card = _j(Path(rc_path), {})\n    summary = str(card.get('summary') or '').strip()\n    if not summary:\n        bullets = card.get('summary_bullets') or []\n        if isinstance(bullets, list):\n            summary = ' '.join(str(x) for x in bullets[:3] if str(x).strip())\n    if not summary:\n        summary = str(card.get('concept') or item.get('title') or '').strip()\n    author = str(ch.get('name') or 'youtube')\n    source = f'https://www.youtube.com/watch?v={item.get("video_id","")}'\n    body = '\\n'.join([\n        f"## {item.get('title', item.get('video_id', 'Untitled'))}",\n        f"*{author} | youtube | {datetime.now(UTC).strftime('%Y-%m-%d')}*",\n        f"Source: {source}",\n        '',\n        f"**Summary:** {summary}",\n    ])\n    return {\n        'video_id': str(item.get('video_id') or ''),\n        'channel_name': author,\n        'feed_target': _digest_target_for_channel(ch),\n        'content_category': content_category,\n        'source': source,\n        'title': str(item.get('title') or item.get('video_id') or ''),\n        'ts_iso': datetime.now(UTC).isoformat(),\n        'body': body,\n    }\n\n\ndef _append_digest_entry(entry: dict):\n    target = str(entry.get('feed_target') or 'quandalf')\n    if target == 'oragorn':\n        path = ORAGORN_DIGEST_PATH\n        title = '# Oragorn Digest'\n        subtitle = 'Rolling cap: 25 AI/architecture entries. Shorts (<90s) and livestreams (>3600s) filtered.'\n    else:\n        path = QUANDALF_DIGEST_PATH\n        title = '# Research Digest — Trading Only'\n        subtitle = 'Rolling cap: 25 trading research entries. Shorts (<90s) and livestreams (>3600s) filtered.'\n    entries = _read_digest_entries(path)\n    vid = str(entry.get('video_id') or '')\n    entries = [e for e in entries if str(e.get('video_id') or '') != vid]\n    entries.insert(0, entry)\n    _write_digest_entries(path, title, subtitle, entries[:DIGEST_ENTRY_CAP])\n\n\ndef _prune_recent_failures(ch: dict, now_ts: float) -> list[str]:\n    history = ch.get('fetch_fail_history') or []\n    out = []\n    for ts in history:\n        dt = _iso_to_dt(str(ts or ''))\n        if dt and (now_ts - dt.timestamp()) <= FETCH_FAIL_WINDOW_SECONDS:\n            out.append(dt.isoformat())\n    return out\n\n\n'''
s = s.replace(anchor, insert)

s = s.replace(
"        row = {\n            'name': name,\n            'url': url,\n            'mode': mode,\n            'enabled': True,\n        }\n",
"        row = {\n            'name': name,\n            'url': url,\n            'mode': mode,\n            'enabled': True,\n            'feed_target': 'quandalf',\n        }\n")

s = s.replace(
"            # reset fetch-fail streak on successful fetch\n            ch['fetch_fail_count'] = 0\n            ch.pop('last_fetch_fail_at', None)\n            ch.pop('last_fetch_fail_detail', None)\n            ch.pop('disabled_reason', None)\n            ch.pop('last_notified_fail_count', None)\n",
"            # reset fetch-fail streak on successful fetch\n            ch['fetch_fail_count'] = 0\n            ch['fetch_fail_history'] = []\n            ch.pop('last_fetch_fail_at', None)\n            ch.pop('last_fetch_fail_detail', None)\n            ch.pop('disabled_reason', None)\n            ch.pop('last_notified_fail_count', None)\n")

s = s.replace(
"        except Exception as e:\n            failed += 1\n            fail_count = int(ch.get('fetch_fail_count', 0) or 0) + 1\n            ch['fetch_fail_count'] = fail_count\n            ch['last_fetch_fail_at'] = datetime.now(UTC).isoformat()\n            ch['last_fetch_fail_detail'] = str(e)[:260]\n            _log('YT_WATCH_FETCH_FAIL', 'YT_WATCH_FETCH_FAIL', f\"channel={channel_id} detail={str(e)[:180]} fail_count={fail_count}\", 'WARN')\n\n            # Notify operator only after second consecutive failure, and then only on state change.\n            last_notified_fail_count = int(ch.get('last_notified_fail_count', 0) or 0)\n            if fail_count == 2 and last_notified_fail_count < 2:\n                _notify_watch_fetch_fail(str(ch.get('name', 'unknown')), channel_id, str(e), fail_count, disabled=False)\n                ch['last_notified_fail_count'] = 2\n\n            if fail_count >= YT_FETCH_FAIL_DISABLE_THRESHOLD:\n                ch['enabled'] = False\n                ch['disabled_reason'] = f\"AUTO_DISABLED_FETCH_FAIL_{fail_count}\"\n                _log('YT_CHANNEL_AUTO_DISABLED', 'YT_CHANNEL_AUTO_DISABLED', f\"channel={channel_id} fail_count={fail_count}\", 'WARN')\n                if last_notified_fail_count < fail_count:\n                    _notify_watch_fetch_fail(str(ch.get('name', 'unknown')), channel_id, str(e), fail_count, disabled=True)\n                    ch['last_notified_fail_count'] = fail_count\n\n            continue\n",
"        except Exception as e:\n            failed += 1\n            now_ts = datetime.now(UTC).timestamp()\n            history = _prune_recent_failures(ch, now_ts)\n            history.append(datetime.now(UTC).isoformat())\n            fail_count = len(history)\n            ch['fetch_fail_history'] = history\n            ch['fetch_fail_count'] = fail_count\n            ch['last_fetch_fail_at'] = datetime.now(UTC).isoformat()\n            ch['last_fetch_fail_detail'] = str(e)[:260]\n            _log('YT_WATCH_FETCH_FAIL', 'YT_WATCH_FETCH_FAIL', f\"channel={channel_id} detail={str(e)[:180]} fail_count={fail_count}\", 'WARN')\n\n            last_notified_fail_count = int(ch.get('last_notified_fail_count', 0) or 0)\n            if fail_count == 2 and last_notified_fail_count < 2:\n                _notify_watch_fetch_fail(str(ch.get('name', 'unknown')), channel_id, str(e), fail_count, disabled=False)\n                ch['last_notified_fail_count'] = 2\n\n            if fail_count >= YT_FETCH_FAIL_DISABLE_THRESHOLD:\n                ch['enabled'] = False\n                ch['disabled_reason'] = f\"AUTO_DISABLED_FETCH_FAIL_{fail_count}_WITHIN_24H\"\n                _log('YT_CHANNEL_AUTO_DISABLED', 'YT_CHANNEL_AUTO_DISABLED', f\"channel={channel_id} fail_count={fail_count} window=24h\", 'WARN')\n                if last_notified_fail_count < fail_count:\n                    _notify_watch_fetch_fail(str(ch.get('name', 'unknown')), channel_id, str(e), fail_count, disabled=True)\n                    ch['last_notified_fail_count'] = fail_count\n\n            continue\n")

# append digest entry in direct process branch
s = s.replace(
"            created.append(str(bpath).replace('\\\\', '/'))\n",
"            created.append(str(bpath).replace('\\\\', '/'))\n            _append_digest_entry(_build_digest_entry(ch, item, rc['research_card_path'], content_category))\n",
1)

# append digest entry in watch-all branch
old = "            created.append(str(bpath).replace('\\\\', '/'))\n            if bundle_status == 'NEW' and content_category in CONCEPT_CATEGORIES:\n                concept_cards.append(str(rc['research_card_path']).replace('\\\\', '/'))\n"
new = "            created.append(str(bpath).replace('\\\\', '/'))\n            _append_digest_entry(_build_digest_entry(ch, item, rc['research_card_path'], content_category))\n            if bundle_status == 'NEW' and content_category in CONCEPT_CATEGORIES:\n                concept_cards.append(str(rc['research_card_path']).replace('\\\\', '/'))\n"
s = s.replace(old, new, 1)

p.write_text(s, encoding='utf-8')
print('patched watcher')
